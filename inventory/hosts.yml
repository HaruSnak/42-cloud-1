all:
  children:
    clouds:
      hosts:
        cloud1:
          ansible_host: X.X.X.X
          ansible_user: XXXX
          ansible_become_pass: "{{ become_password }}"
      vars:
        ansible_python_interpreter: /usr/bin/python3